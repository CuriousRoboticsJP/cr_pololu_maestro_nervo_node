services:
  ros2_humble:
    build:
      context: .
      dockerfile: DockerFile
    privileged: true
    ipc: host
    pid: host
    network_mode: host
    stdin_open: true
    tty: true
    environment:
      - DISPLAY=host.docker.internal:0.0
    volumes:
      - ..:/root/ros_ws/src/
      - /tmp/.X11-unix:/tmp/.X11-unix
